# WARP.md

This file provides guidance to WARP (warp.dev) when working with code in this repository.

## Repository Overview

This is the `realtime-local-starter` repository - a starter template for real-time local development. The codebase is currently in its initial setup phase.

## Code Ownership

- `/gitops/` and `/security/` directories are owned by `@org/platform` (see `.github/CODEOWNERS`)

## Security

- Security policy is defined in `SECURITY.md`
- Supported versions and vulnerability reporting procedures are documented there
